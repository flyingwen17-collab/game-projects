# Claude Code 遊戲製作流程 — 最終版（2D / 3D 通用）

> 版本：v2.0（2026-08-04）
> 定位：**本專案所有遊戲的「怎麼做」總綱**。各遊戲的「做什麼」看各自的企劃書（甩尾、蚯蚓）。兩者衝突時，以本文件為準。
> 已對照業界流程撰寫（來源見第 1 節），並針對「一人 + Claude Code + ChatGPT 生圖」的組合精簡化。
> 核心承諾：AI 生圖**直接進遊戲當正式素材**（不只是參考圖）；每個功能過四道品保關卡才算完成。

---

## 0. 三條最高原則（背下來）

1. **玩法先於美術**：灰盒驗證「好玩」之前，一張美術圖都不做。這是所有業界流程的共識，也是獨立開發最常死的地方。
2. **每一步都可回退**:功能完成就 commit，里程碑完成就打 tag。改壞了 5 分鐘內回得去。
3. **驗收才算完成**：程式「寫完了」不算完成，要過第 5 節的四道關卡（編譯 → 截圖 → 實玩 → 提交）才算。

---

## 1. 業界流程對照（我們的流程從哪裡來）

### 1.1 業界標準流程（3A ～ 中型團隊）

```
① 概念/立項 → ② 前期製作(原型) → ③ 垂直切片 → ④ 量產 → ⑤ 測試QA → ⑥ 發行 → ⑦ 營運
```

- **前期製作（Pre-production）**：用最便宜的方式證明「核心機制好玩」。產出是可玩原型，不是美術。
- **垂直切片（Vertical Slice）**：一段「從選單玩到結算」的完整流程，品質接近成品，長度幾分鐘即可。它是整個專案的品質標尺。
- **量產（Production）**：最長的階段。關卡、敵人、素材、音效在這裡批量生產。
- **QA**：業界做法是「功能做完的當個迭代就測，不堆到最後」；每修一個 bug 要回測鄰近系統（迴歸測試）。

### 1.2 獨立 / 一人開發的實際做法

- 前期製作壓縮到數週；原型和量產經常交錯進行。
- 一人身兼多職 → 必須靠**流程紀律**（里程碑驗收、版本控制、檢查清單）取代團隊裡的 QA 和製作人。
- 自己測自己的遊戲一定會漏 bug——因為你只會用「正確的方式」玩。所以要有清單逼自己用錯誤的方式玩（見 5.3）。

### 1.3 AI 素材管線的業界共識（2026 現況）

- **原始 AI 輸出幾乎不能直接用**，可用的是一條管線：**生成 → 後處理清理 → 按規則匯入 → 進引擎驗證**。
- 用**固定資料夾結構 + 批次編號**管理 AI 素材，**保存每張圖的 prompt**（風格漂移時才能重現）。
- 每張圖進遊戲前要有**人工審核關卡**（在我們的流程裡就是你拍板）。

### 1.4 我們的精簡版（本文件採用）

```
P0 一頁企劃 → P1 灰盒原型 → P2 垂直切片(首批AI圖進遊戲) → P3 內容量產 → P4 打磨 → P5 發布
```

跟業界七階段的對應：把「概念+立項」壓成一頁文件（P0）、「測試」不獨立成階段而是拆進每一個功能的四道關卡（第 5 節）、營運階段不做。

---

## 2. 分工表（誰做什麼）

| 工作 | Claude（我） | 你 |
|------|-------------|-----|
| 企劃文件、系統設計 | ✅ 起草 | ✅ 拍板 |
| 全部程式（C#、Shader、Editor 工具） | ✅ | 按 Play |
| 場景/材質/光照/後處理 | ✅ 寫成一鍵腳本或直接改 .asset | 確認畫面 |
| **編譯驗證與截圖** | ✅ **我自己跑 Unity batch mode，不需要你截圖**（見 5.1、5.2） | — |
| ChatGPT 生圖 | ✅ 開規格單 + 下 Codex CLI 指令生圖 | 也可自己在 ChatGPT 生，丟進 `_Refs\` |
| AI 圖後處理（去背/切圖/匯入設定） | ✅ 全自動腳本 | — |
| 素材審美拍板 | 給選項與建議 | ✅ **最終決定權** |
| 實機試玩（手感、好不好玩） | 不能代替 | ✅ **只有你能做** |
| CC0 資源下載 | 給精確網址檔名 | ✅ 下載 |
| Git 提交、tag、進度更新 | ✅ | — |

---

## 3. 六階段流程（2D / 3D 通用）

每個階段都有：**目標、產出、驗收條件**。驗收沒過不進下一階段。

### P0 — 一頁企劃（半天～1 天）

- 產出一份**一頁文件**回答四題：
  1. 核心循環一句話（例：「甩尾累積分數 → 解鎖 → 再挑戰」）
  2. 2D 還是 3D？（判斷標準見下表）
  3. 三根柱子（這遊戲好玩的三個理由，之後所有功能都要能掛到柱子上，掛不上的不做）
  4. 目標（自玩 / 作品集 / itch.io / Steam）
- **2D vs 3D 決策表**：

| 條件 | 選 2D | 選 3D |
|------|-------|-------|
| 核心樂趣靠鏡頭/空間感（競速、追逐） | | ✅ |
| 核心樂趣靠精準操作/讀畫面（平台、彈幕、解謎） | ✅ | |
| ChatGPT 生圖能當多少正式素材 | **幾乎全部**（角色 sprite、背景、UI） | 貼圖/粒子/UI/貼花（模型做不了） |
| 一人完成的美術量 | 低很多 | 中～高（靠 CC0 模型補） |

- **驗收：一頁文件你點頭。**

### P1 — 灰盒原型（1～2 週）

- 只做核心機制。2D 用色塊方塊，3D 用 Primitive 灰盒。**零美術、零 UI 美化、零音效**（除非音效就是核心機制）。
- 所有可調參數集中在 ScriptableObject，調手感不改程式碼。
- **驗收：灰盒狀態下你玩 10 分鐘覺得「有東西」。不好玩就在這裡改或砍掉重練——這是全流程最便宜的砍點。**

### P2 — 垂直切片（2～3 週）★ 品質標尺

- 做出**一段完整流程**：主選單 → 玩一關 → 結算 → 重試。
- **首批 ChatGPT 圖在這裡進遊戲**（管線見第 4 節）：先做 UI + 粒子 +（2D 的話）主角與場景 sprite。
- 3D 專案在此階段同時完成 M0 光照後處理（ACES、Bloom、HDRI 天空、陰影）——畫面質感 50% 來自這裡。
- **驗收：這一段的截圖可以拿去給陌生人看，對方能看懂並想玩。**

### P3 — 內容量產（2～4 週）

- 照垂直切片的品質標準複製：更多關卡、敵人、模式。
- AI 圖**按批次**量產（一批 = 同場景/同類素材一次生完，避免風格漂移）。
- 每做完一個內容單位（一關/一種敵人）就走一次四道關卡。
- **驗收：企劃書列的內容清單全部打勾。**

### P4 — 打磨（1～2 週）

- 特效、音效、鏡頭語言、UI 動畫、難度曲線、教學引導。
- 效能：Profiler、貼圖壓縮、（3D）LOD 與 GPU Instancing、（2D）Sprite Atlas。
- **驗收:目標機器穩定 60 FPS；連續玩 15 分鐘無報錯（Console 乾淨）。**

### P5 — 發布（1 週）

- Windows Build 打包 → 在**非開發資料夾**跑一次完整流程（存檔讀檔、視窗切換、Alt+Tab）。
- `CREDITS.md` 核對完整 → 上架 itch.io（選配）。
- **驗收：別人的電腦能玩。**

---

## 4. ChatGPT 生圖 → 進遊戲 管線（核心章節）

> 原則：**AI 圖是正式素材，不是參考圖**。但「生出來」到「能進遊戲」中間有一條必經管線，跳過任何一步就會出現風格不一、白邊、模糊、雙重打光等經典災難。

### 4.1 哪些素材可以直接進遊戲（可用性分級）

| 素材 | 2D 遊戲 | 3D 遊戲 | 備註 |
|------|---------|---------|------|
| UI 圖示 / 按鈕 / 邊框 | ✅ 正式素材 | ✅ 正式素材 | 一批生完保風格；文字用 TextMeshPro 疊 |
| 粒子貼圖（煙/火花/光暈） | ✅ | ✅ | 純黑底灰階 → 腳本轉 alpha，成功率最高 |
| 主角 / 敵人 | ✅ **sprite 直接用**（多動作分開生+人工挑） | ❌ 模型做不了 → CC0 模型 | |
| 場景背景 | ✅ 分層背景（遠/中/近景）直接用 | △ 只能當遠景告示牌(billboard) | |
| 地表材質 | △ 風格化手繪 tile 可以 | △ 無縫平鋪不可靠（實測草地/泥土有接縫）→ 優先 ambientCG CC0 | |
| 貼花 Decal（塗鴉/裂痕/水漬） | ✅ | ✅ | 要去背 |
| 天空 | ✅ 漸層天空可 | △ 全景 HDRI 接縫做不到 → Poly Haven | |
| 宣傳圖 / 商店封面 | ✅ | ✅ | 標題文字另外疊 |
| 3D 模型 | — | ❌ | AI 圖是平面的，靠 CC0 + Blender 腳本 |

### 4.2 五步管線（每批素材都走這條）

```
① 開規格單 → ② 生圖 → ③ 入庫 → ④ 自動後處理 → ⑤ 進遊戲驗收
```

**① 我開規格單**。每批素材我先給你一張表：素材名、用途、尺寸、背景色要求、要幾張、完整英文 prompt（含風格鎖句）。你不用自己想 prompt。

**② 生圖**（兩條路，效果相同，都走你的 ChatGPT 訂閱、不花 API 錢）：
- **A 我來生（預設）**：我直接下 Codex CLI 指令批次生圖，生完自動存到正確資料夾。
- **B 你來生**：你把規格單的 prompt 貼進 ChatGPT，產出的圖存到 `_Refs\` 對應資料夾後告訴我。
- **鐵則：同一批素材一次生完**。分五次生的五個圖示放一起像五個遊戲的東西。

**③ 入庫**。原始圖進 `_Refs\`（見 7.2 的資料夾結構），檔名照 `批次_用途_序號.png`。同時把該批 prompt 記進 `_Refs\PROMPTS.md`——風格漂移時要靠它重現。

**④ 我跑自動後處理**（Unity Editor 腳本，一鍵）：
- 黑底粒子圖 → 亮度轉 alpha 透明
- 白底圖示 → 去背 + 裁切置中
- 尺寸規整為 2 的次方（512 / 1024 / 2048）
- 2D sprite：統一 PPU、設 Filter Mode（像素風用 Point）、打進 Sprite Atlas
- 設定壓縮格式與匯入參數
- 處理完的成品存 `Assets/_Project/Art/`，**原圖永遠留在 `_Refs\` 不刪**（成品壞了可重跑）

**⑤ 進遊戲驗收**。我把素材掛進場景 → batch mode 算圖截圖給你看 → **你拍板**。不過關就回到 ① 改規格單重生，不要在遊戲裡硬調。

### 4.3 風格一致性三件套（AI 圖最大的坑）

1. **風格鎖句**：專案定案一段固定的風格描述（例：`stylized low-poly, cel-shaded flat colors, warm palette, thick outlines`），**每個 prompt 結尾都貼同一段**，存在 `_Refs\PROMPTS.md` 最上面。
2. **同批生產**：同場景/同介面的素材永遠一批生完。
3. **對照驗收**：每批新素材進遊戲後，跟舊素材同框截圖比一次；風格對不上就整批重生，不要混用。

### 4.4 通用 prompt 約束（每個指令都要帶）

| 要什麼 | 加什麼 |
|--------|--------|
| 無烘焙光影（進引擎再打光） | `flat even lighting, no shadows, no highlights` |
| 無透視（材質/俯視素材） | `orthographic view, no perspective` |
| 好去背 | 粒子 `pure black background`；圖示 `plain white background` |
| 正方形 | `1:1 square aspect ratio` |
| 乾淨 | `no text, no watermark, no border, no vignette, no signature` |

> 詳細的分類 prompt 範本（粒子/圖示/材質/宣傳圖各一套）在 [AI作圖指令模板.md](AI作圖指令模板.md)，本文件不重複。

### 4.5 授權與記錄

- ChatGPT/OpenAI 條款：生成圖所有權歸使用者，可商用。但**核心視覺仍建議 AI + CC0 混用**，別讓整包美術繫在單一平台的條款上。
- CC0 資源照舊規則：只碰 CC0，CC-BY 記 credits，NC 絕不碰。
- **每批素材入庫當下**在 `CREDITS.md` 記一筆（AI 生成也記：工具、日期、批次名）。

---

## 5. 防 BUG 品保流程（四道關卡）

> 誠實聲明：世界上沒有「零 bug」的遊戲，3A 也沒有。能做到的是——**bug 在寫完當下就被抓住，而不是累積到最後炸開**。業界共識是「功能完成的那個迭代就要測」，以下四道關卡把這件事變成強制流程。

### 關卡 1 — 編譯驗證（我做，每次改完程式）

- 我用 Unity batch mode **不開編輯器**直接編譯整個專案，讀 log 抓編譯錯誤，自己修到乾淨為止。
- 你永遠不會收到「我寫完了」但一按 Play 就滿屏紅字的東西。

### 關卡 2 — 畫面驗證（我做，涉及畫面的改動）

- 我用 batch mode 進場景算圖截圖，自己先看：素材有沒有掛上、位置對不對、光照有沒有炸。
- 有問題我先修，修完的截圖才給你過目。

### 關卡 3 — 實玩檢查清單（你做，每個功能完成時）

我每交付一個功能，會附一張**該功能的 5 分鐘檢查清單**，固定包含三類：

1. **正常路徑**：照設計玩一次，行為符合預期。
2. **故意亂玩**（最重要——你自己玩永遠用「對的方式」，bug 都藏在錯的方式裡）：
   - 在邊界/牆角/斜坡做這個動作
   - 動作做到一半按 Esc 暫停、切視窗、再回來
   - 連打按鍵、同時按互斥的鍵、在讀取中狂按
   - 數值推極限（速度最快時、體力歸零時、分數溢出時）
3. **回歸抽查**：這次改動**鄰近的舊功能**挑 2 項重玩（修 A 弄壞 B 是最常見的 bug 來源）。

發現問題 → 用一句話記進 `BUGS.md`（現象 + 怎麼觸發），我來修。**修完的 bug 我會回測清單上的鄰近項目才交還。**

### 關卡 4 — 提交與回退（我做，通過前三關後）

- 通過驗收 → `git commit`（訊息寫清楚做了什麼）。
- 里程碑完成 → `git tag P2-vertical-slice` 之類。
- 任何時候改壞了：回上一個 commit，最多損失一個功能的工時。

### 節奏規則

- **小步提交**：一個 commit 只做一件事。一次改十個系統 = bug 來源查不出來。
- **Console 零容忍**：黃色警告也要清。警告堆多了，真正的紅字出現時你會看不到。
- **不帶病前進**：`BUGS.md` 有未修的「會壞流程」級 bug 時，不開新功能。

---

## 6. 素材技術規格（鐵則）

### 6.1 共通

- 貼圖一律 **2 的次方**（512 / 1024 / 2048），非 2 次方的圖在匯入時規整。
- 檔名英文小寫 + 底線：`tire_smoke_01.png`。中文檔名會在工具鏈炸掉。
- 成品素材放 `Assets/_Project/`，原始檔（AI 原圖、Blender 源檔）放專案外的 `_Refs\`，Unity 專案不吃原始檔。

### 6.2 2D 專用

- **全專案統一 PPU**（Pixels Per Unit），開工時定一次（像素風慣例：PPU = tile 尺寸），之後不改。
- 像素風：Filter Mode = **Point**，Compression = None；手繪風：Bilinear 可。
- 同場景的 sprite 進 **Sprite Atlas**（省 draw call，也統一管理 Max Size）。
- 分層背景（視差捲動）每層獨立一張，比一張大圖好維護。

### 6.3 3D 專用

- 燈光後處理先行（Linear 色彩空間、HDR、ACES Tonemapping、HDRI 環境光）——畫面質感 50% 在這，先做這個再談素材。
- 材質統一管理 Metallic / Smoothness，不放預設值。
- 風格統一 > 模型精細：全低模 > 「主角精細背景方塊」。
- 模型來源優先序：CC0（Kenney / Quaternius / Poly Haven）→ Blender 腳本生成 → 手工建模。

---

## 7. 檔案與資料夾規範

### 7.1 專案根目錄（`E:\下載\CLAUDE\ｇａｍｅ\`）

```
Claude-Code遊戲製作流程-最終版.md   ← 本文件（怎麼做）
<遊戲名>企劃書.md                    ← 各遊戲（做什麼），頂部必有「狀態欄」
AI作圖指令模板.md                    ← prompt 範本庫
CREDITS.md                          ← 素材來源記錄（含 AI 批次）
BUGS.md                             ← 未修 bug 清單
_Refs\                              ← AI 原圖 + 參考圖 + PROMPTS.md
<UnityProject>\                     ← 各 Unity 專案（英文名）
```

### 7.2 `_Refs\` 結構

```
_Refs\PROMPTS.md        ← 風格鎖句 + 每批素材的完整 prompt
_Refs\01_風格參考\  02_粒子貼圖\  03_UI圖示\  04_材質貼圖\  05_天空盒\  06_宣傳圖\
_Refs\07_角色sprite\  08_場景背景\        ← 2D 專案加開
```

### 7.3 文件維護規則（上次檢討的教訓）

1. **狀態欄要活的**：每份企劃書頂部的狀態（企劃中/P1/P2…）和里程碑 checkbox，做完當下就更新。文件跟現實脫節比沒有文件更糟。
2. **決定要回填**：任何「待你決定」的事項有了答案，當天寫回文件，附日期。
3. **環境事實只記一處**：Unity 版本、路徑等記在專案 CLAUDE.md（單一事實來源），企劃書不重複記。
4. **記錄當下寫**：CREDITS / PROMPTS / BUGS 都是「事情發生當下記一筆」，事後補一定漏。

---

## 8. 每次開工的標準迴圈

每個工作階段（session）固定這樣走：

```
1. 我讀進度（企劃書狀態欄 + git log + BUGS.md）→ 跟你確認今天目標
2. 動工（我寫程式/生圖/跑管線）
3. 關卡 1+2（我自己編譯 + 截圖驗證）
4. 交付：給你「改了什麼 + 截圖 + 5 分鐘檢查清單」
5. 你實玩回報 → 有 bug 我修（修完回測）
6. 通過 → commit → 更新企劃書狀態欄 → 收工
```

### 你可以用的口令

| 你說 | 我做 |
|------|------|
| 「開始 P〇」 | 從該階段第一項動工 |
| 「生圖：〇〇」 | 開規格單 → Codex CLI 批次生圖 → 跑後處理管線 |
| 「驗收」 | 跑編譯 + 批次截圖給你看 |
| 「有 bug：〇〇」 | 記進 BUGS.md → 修 → 回測 → 回報 |
| 「進度」 | 報目前階段、已完成、下一步 |
| 「存檔」 | commit + 必要時打 tag |

---

## 9. 風險對照表（濃縮版）

| 風險 | 對策（本文件哪一節擋它） |
|------|--------------------------|
| 不好玩才發現，美術白做 | P1 灰盒驗收先於一切美術（§3） |
| AI 圖風格漂移，像雜牌拼盤 | 風格鎖句 + 同批生產 + 對照驗收（§4.3） |
| bug 堆到最後炸開 | 每功能四道關卡，不帶病前進（§5） |
| 改壞回不去 | 小步 commit + 里程碑 tag（§5 關卡 4） |
| 範圍膨脹做不完 | 三根柱子過濾功能 + 階段驗收制（§3） |
| 文件與現實脫節 | 狀態欄活更新 + 決定回填（§7.3) |
| 授權地雷 | CC0 優先 + CREDITS 當下記（§4.5） |
| 兩專案並行兩個都爛尾 | 一次只推一個專案的階段進度 |

---

## 10. 現在就能做的第一步

本文件生效後的建議順序：

1. **「存檔」**——目前整個 repo 還沒有任何 commit，先把現況提交當保險（5 分鐘）。
2. 建立 `CREDITS.md`、`BUGS.md`、`_Refs\PROMPTS.md` 三個空殼 + 把現有 `_Refs\` 素材補記（30 分鐘）。
3. 更新兩份企劃書的狀態欄到真實進度（10 分鐘）。
4. 然後照第 8 節的迴圈，從當前專案的當前階段繼續推進。

回覆「照第一步做」我就從 1 開工。
