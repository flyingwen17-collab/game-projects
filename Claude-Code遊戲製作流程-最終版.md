# Claude Code 遊戲製作流程 — 最終版（2D / 3D 通用）

> 版本：v3.0（2026-08-08：重構。新增畫質檔位決策、AI 3D 生成管線、Blender 後處理、物理鐵則、效能關卡、音訊管線、備份規範）
> 前版：v2.1（2026-08-04：新增 CC0 3D 模型管線）
> 定位：**本專案所有遊戲的「怎麼做」總綱**。各遊戲的「做什麼」看各自的企劃書。兩者衝突時，以本文件為準。
> 核心承諾：AI 素材**直接進遊戲當正式素材**；每個功能過五道品保關卡才算完成。

---

## 0. 四條最高原則（背下來）

1. **玩法先於美術**：灰盒驗證「好玩」之前，一張美術圖都不做。這是所有業界流程的共識，也是獨立開發最常死的地方。
2. **先定檔位再談畫質**：不先決定「要多真」，所有美術決策都會亂（v3.0 新增，見 §3 P0）。
3. **每一步都可回退**：功能完成就 commit，里程碑完成就 tag，收工就 push 到遠端。改壞了 5 分鐘內回得去，硬碟壞了還在。
4. **驗收才算完成**：程式「寫完了」不算完成，要過 §5 的五道關卡才算。

---

## 1. 業界流程對照（我們的流程從哪裡來）

### 1.1 業界標準流程（3A ～ 中型團隊）

```
① 概念/立項 → ② 前期製作(原型) → ③ 垂直切片 → ④ 量產 → ⑤ 測試QA → ⑥ 發行 → ⑦ 營運
```

- **前期製作**：用最便宜的方式證明「核心機制好玩」。產出是可玩原型，不是美術。
- **垂直切片**：一段「從選單玩到結算」的完整流程，品質接近成品，長度幾分鐘即可。它是整個專案的品質標尺。
- **量產**：最長的階段。關卡、敵人、素材、音效在這裡批量生產。
- **QA**：業界做法是「功能做完的當個迭代就測，不堆到最後」；每修一個 bug 要回測鄰近系統。

### 1.2 獨立 / 一人開發的實際做法

- 前期製作壓縮到數週；原型和量產經常交錯進行。
- 一人身兼多職 → 必須靠**流程紀律**取代團隊裡的 QA 和製作人。
- 自己測自己的遊戲一定會漏 bug——因為你只會用「正確的方式」玩。所以要有清單逼自己用錯誤的方式玩（見 5.4）。

### 1.3 AI 素材管線的業界共識（2026 現況）

- **原始 AI 輸出幾乎不能直接用**，可用的是一條管線：**生成 → 後處理清理 → 按規則匯入 → 進引擎驗證**。
- 用**固定資料夾結構 + 批次編號**管理 AI 素材，**保存每張圖的 prompt**。
- 每張圖進遊戲前要有**人工審核關卡**（在我們的流程裡就是你拍板）。
- **2026 新增事實：image-to-3D 已經是可用的生產工具**（Tripo、Meshy、開源的 Hunyuan3D / TRELLIS），不再像 2025 只能玩玩。但它是**補位工具不是主力**（原因與規則見 §4.6）。
- 2026 一人開發的典型組合：程式用 AI coding agent、2D 用生圖、3D 用「CC0 為主 + AI 生成補缺」、音樂音效用 AI 生成。

### 1.4 我們的精簡版（本文件採用）

```
P0 一頁企劃(含畫質檔位) → P1 灰盒原型 → P2 垂直切片(首批素材進遊戲) → P3 內容量產 → P4 打磨 → P5 發布
```

---

## 2. 分工表（誰做什麼）

| 工作 | Claude（我） | 你 |
|------|-------------|-----|
| 企劃文件、系統設計 | ✅ 起草 | ✅ 拍板 |
| 全部程式（C#、Shader、Editor 工具） | ✅ | 按 Play |
| 場景/材質/光照/後處理 | ✅ 寫成一鍵腳本或直接改 .asset | 確認畫面 |
| **物理參數調校** | ✅ 改設定 + 跑自測場景出數據 | ✅ **手感好不好只有你能判** |
| **編譯驗證、截圖、效能量測** | ✅ **我自己跑 Unity batch mode** | — |
| AI 生圖 | ✅ 開規格單 + 下指令生圖 | 也可自己生，丟進 `_Refs\` |
| AI 圖後處理（去背/切圖/匯入設定） | ✅ 全自動腳本 | — |
| **AI 3D 生成（Tripo/Meshy）** | ✅ 開規格 + 給操作步驟 + 後處理 | ✅ 網頁端操作（免費額度） |
| **Blender headless 後處理** | ✅ 全腳本 | — |
| 素材審美拍板 | 給選項與建議 | ✅ **最終決定權** |
| 實機試玩（手感、好不好玩） | 不能代替 | ✅ **只有你能做** |
| CC0 / Fab 資源下載 | 給精確網址檔名 | ✅ 下載 |
| Git 提交、tag、**push 遠端**、進度更新 | ✅ | 一次性設定遠端 |

---

## 3. 六階段流程（2D / 3D 通用）

每個階段都有：**目標、產出、驗收條件**。驗收沒過不進下一階段。

### P0 — 一頁企劃（半天～1 天）

產出一份**一頁文件**回答五題：

1. 核心循環一句話（例：「甩尾累積分數 → 解鎖 → 再挑戰」）
2. 2D 還是 3D？
3. **畫質檔位是哪一檔？**（v3.0 新增，見下表）
4. 三根柱子（這遊戲好玩的三個理由；之後所有功能都要能掛到柱子上，掛不上的不做）
5. 目標（自玩 / 作品集 / itch.io / Steam）

**2D vs 3D 決策表**：

| 條件 | 選 2D | 選 3D |
|------|-------|-------|
| 核心樂趣靠鏡頭/空間感（競速、追逐） | | ✅ |
| 核心樂趣靠精準操作/讀畫面（平台、彈幕、解謎） | ✅ | |
| AI 生圖能當多少正式素材 | **幾乎全部** | 貼圖/粒子/UI/貼花 |
| 一人完成的美術量 | 低很多 | 中～高 |

**畫質檔位決策表（3D 專用，v3.0 新增）**：

| 檔位 | A 風格化低模 | **B 半寫實（本專案預設）** | C 照片寫實 |
|------|------|------|------|
| 長相 | 卡通比例、平塗、粗描邊 | 真實比例 + PBR 掃描材質 + 電影級光照，幾何與色彩風格化 | 掃描資產 + 光追 |
| 模型來源 | Kenney / Quaternius | Fab 免費 Megascans + Poly Haven + AI 生成補缺 | 高階掃描庫 |
| 引擎管線 | URP | **URP + 完整光照後處理** | HDRP |
| 三角面預算（單物件） | 500～3k | **3k～15k（含 LOD）** | 50k+ |
| 貼圖尺寸 | 512～1024 | **1024～2048** | 4K～8K |
| 這台機器（GTX 1060 6GB / i7-7700 / 16GB）跑得動嗎 | 非常輕鬆 | **可以，穩 60fps** | ❌ 無 RT core、VRAM 不足，開發會很痛 |

> **2026-08-08 決議：3D 專案一律走 B 檔（半寫實）**。理由：C 檔在這台機器上不是預算問題是硬體天花板，而且「不夠真」比「不真」更難看；B 檔是一人開發拿得出手的甜蜜點。
> 檔位一旦定下，§4 的素材來源、§6.3 的規格、§6.1 的預算全部跟著這一欄走。**中途換檔位＝美術重做**，要換必須明確走一次 P0。

**效能預算（P0 就定，不是 P4 才想，v3.0 新增）**：見 §6.1。

- **驗收：一頁文件你點頭。**

### P1 — 灰盒原型（1～2 週）

- 只做核心機制。2D 用色塊方塊，3D 用 Primitive 灰盒。**零美術、零 UI 美化、零音效**（除非音效就是核心機制）。
- 所有可調參數集中在 ScriptableObject，調手感不改程式碼。
- **物理遊戲加一條**：核心物理系統要在這裡就寫好 headless 自測場景（§5.3），灰盒階段的物理數據就是之後所有調校的基準線。
- **驗收：灰盒狀態下你玩 10 分鐘覺得「有東西」。不好玩就在這裡改或砍掉重練——這是全流程最便宜的砍點。**

### P2 — 垂直切片（2～3 週）★ 品質標尺

- 做出**一段完整流程**：主選單 → 玩一關 → 結算 → 重試。
- **首批素材在這裡進遊戲**（管線見 §4）：先做 UI + 粒子 +（2D）主角與場景 sprite /（3D）主要模型與材質。
- 3D 專案在此階段同時完成 **M0 光照後處理全套**（§6.3 的清單逐項打勾）——畫面質感 50% 來自這裡。
- **效能第一次驗收**（v3.0 新增）：垂直切片必須符合 §6.1 的預算。這裡超標，量產後只會更糟。
- **驗收：這一段的截圖可以拿去給陌生人看，對方能看懂並想玩；且效能達標。**

### P3 — 內容量產（2～4 週）

- 照垂直切片的品質標準複製：更多關卡、敵人、模式。
- 素材**按批次**量產（一批 = 同場景/同類素材一次生完，避免風格漂移）。
- 每做完一個內容單位（一關/一種敵人）就走一次五道關卡。
- **驗收：企劃書列的內容清單全部打勾，且效能仍達標。**

### P4 — 打磨（1～2 週）

- 特效、音效、鏡頭語言、UI 動畫、難度曲線、教學引導。
- 效能收尾：Profiler 抓熱點、貼圖壓縮、（3D）LOD 與 GPU Instancing、（2D）Sprite Atlas。
- **驗收：目標機器穩定 60 FPS；連續玩 15 分鐘無報錯（Console 乾淨）。**

### P5 — 發布（1 週）

- Windows Build 打包 → 在**非開發資料夾**跑一次完整流程（存檔讀檔、視窗切換、Alt+Tab）。
- `CREDITS.md` 核對完整（**含所有 CC BY 的標註義務**）→ 上架 itch.io（選配）。
- **驗收：別人的電腦能玩。**

---

## 4. 素材管線（核心章節）

> 原則：**AI 素材是正式素材，不是參考圖**。但「生出來」到「能進遊戲」中間有一條必經管線，跳過任何一步就會出現風格不一、白邊、模糊、雙重打光等經典災難。

### 4.1 素材可用性分級（2026-08-08 更新）

| 素材 | 2D 遊戲 | 3D 遊戲（B 檔） | 備註 |
|------|---------|---------|------|
| UI 圖示 / 按鈕 / 邊框 | ✅ AI 生圖 | ✅ AI 生圖 | 一批生完保風格；文字用 TextMeshPro 疊 |
| 粒子貼圖（煙/火花/光暈） | ✅ AI 生圖 | ✅ AI 生圖 | 純黑底灰階 → 腳本轉 alpha，成功率最高 |
| 主角 / 敵人 | ✅ AI sprite 直接用 | **△ AI 生 3D 補位**（§4.6）或 CC0 | 人形要 auto-rig；動作用 Mixamo |
| 場景背景 | ✅ 分層背景直接用 | △ 只能當遠景 billboard | |
| 地表 / 建築材質 | △ 風格化手繪 tile 可以 | **❌ AI 生圖不做主力 → Fab / Poly Haven / ambientCG 掃描 PBR** | 半寫實的質感命脈在這，別省 |
| 貼花 Decal（塗鴉/裂痕/水漬） | ✅ AI 生圖 | ✅ AI 生圖 | 要去背 |
| 天空 | ✅ 漸層天空可 | ❌ AI 全景有接縫 → **Poly Haven 真 HDRI** | 曝光壓 0.6～0.8 |
| 宣傳圖 / 商店封面 | ✅ AI 生圖 | ✅ AI 生圖 | 標題文字另外疊 |
| **3D 模型** | — | **CC0 為主 + AI image-to-3D 補缺**（§4.5 / §4.6） | v3.0 修訂：舊版寫「AI 做不了」已過期 |
| **音效 / 音樂** | ✅ | ✅ | 見 §4.8 |

### 4.2 五步管線（每批 2D 素材都走這條）

```
① 開規格單 → ② 生圖 → ③ 入庫 → ④ 自動後處理 → ⑤ 進遊戲驗收
```

**① 我開規格單**。每批素材我先給你一張表：素材名、用途、尺寸、背景色要求、要幾張、完整英文 prompt（含風格鎖句）。你不用自己想 prompt。

**② 生圖**。目前預設走你的 ChatGPT 訂閱（我下 Codex CLI 指令批次生，或你自己貼 prompt 生），**不花 API 錢**。
> v3.0 補充：生圖來源刻意寫成可替換的一步。任何能吃 §4.4 約束、輸出品質夠的工具都能接上，換工具只需要改這一步，不動管線其他四步。理由見 §4.9——不要讓整包美術繫在單一平台的條款上。
>
> **鐵則：同一批素材一次生完**。分五次生的五個圖示放一起像五個遊戲的東西。

**③ 入庫**。原始圖進 `_Refs\`，檔名照 `批次_用途_序號.png`。同時把該批 prompt 記進 `_Refs\PROMPTS.md`。

**④ 我跑自動後處理**（Unity Editor 腳本，一鍵）：
- 黑底粒子圖 → 亮度轉 alpha 透明
- 白底圖示 → 去背 + 裁切置中
- 尺寸規整為 2 的次方
- 2D sprite：統一 PPU、設 Filter Mode、打進 Sprite Atlas
- 設定壓縮格式與匯入參數
- 處理完的成品存 `Assets/_Project/Art/`，**原圖永遠留在 `_Refs\` 不刪**

**⑤ 進遊戲驗收**。我把素材掛進場景 → batch mode 算圖截圖給你看 → **你拍板**。不過關就回到 ① 改規格單重生，不要在遊戲裡硬調。

### 4.3 風格一致性三件套（AI 素材最大的坑）

1. **風格鎖句**：專案定案一段固定的風格描述，**每個 prompt 結尾都貼同一段**，存在 `_Refs\PROMPTS.md` 最上面。
   - A 檔範例：`stylized low-poly, cel-shaded flat colors, warm palette, thick outlines`
   - **B 檔範例**：`semi-realistic stylized, physically based materials, realistic proportions, subtle color grading, no outlines`
2. **同批生產**：同場景/同介面的素材永遠一批生完。
3. **對照驗收**：每批新素材進遊戲後，跟舊素材同框截圖比一次；風格對不上就整批重生，不要混用。

### 4.4 通用 prompt 約束（每個指令都要帶）

| 要什麼 | 加什麼 |
|--------|--------|
| 無烘焙光影（進引擎再打光） | `flat even lighting, no shadows, no highlights` |
| 無透視（材質/俯視素材） | `orthographic view, no perspective` |
| 好去背 | 粒子 `pure black background`；圖示 `plain white background` |
| 正方形 | `1:1 square aspect ratio` |
| 乾淨 | `no text, no watermark, no border, no vignette, no signature` |

> 詳細的分類 prompt 範本在 [AI作圖指令模板.md](AI作圖指令模板.md)，本文件不重複。

### 4.5 CC0 素材管線（模型 / 材質 / HDRI）

> AI 圖救不了幾何——車是方塊拼的再好的貼圖也是方塊。3D 專案的模型與材質走這條線。

**來源（依畫質檔位分流，v3.0 更新）**：

| 類型 | A 檔（風格化） | **B 檔（半寫實，本專案）** |
|------|------|------|
| 模型 | Kenney（kenney.nl）、Quaternius | **Fab 永久免費 Megascans starter（約 1500 件掃描 surfaces / decals / 3D scans，任何引擎可用）** + Poly Haven models；Kenney/Quaternius 只用來當佔位 |
| 材質 | Kenney 調色盤貼圖 | **Poly Haven / ambientCG CC0 掃描 PBR**（註：8K 在 16GB RAM 上會吃緊，匯入時降到 2K） |
| 天空 | 漸層 / AI | **Poly Haven 真 HDRI**（`dl.polyhaven.org` API 直接下載） |

- 原始包存 `_Assets\`（已 gitignore；來源記在 CREDITS.md，可重新下載）
- 選用的檔案複製進各專案 `Assets/_Project/Art/<來源名>/`
- 工具：Blender 4.5 可攜版在 `E:\Tools\blender-4.5.12-windows-x64`（含 Python + gdown）

**匯入三件套**（每個 3D 專案一份，抄 DriftGame 的 `KenneyLib.cs` / SumoParty 的 `CrowdAssets.cs`）：

1. **AssetPostprocessor**：FBX 內嵌材質自動轉 URP Lit（不轉整批粉紅）＋開 GPU Instancing
2. **擺放工具**：載入 → 量 Renderer 邊界 → 縮放到目標尺寸 → 底部貼地。**永不假設模型的原始比例或樞紐位置**
3. **場景生成器只換視覺**：物理碰撞體照舊（隱形方塊），模型純當皮——手感零風險

**實戰踩過的坑（血淚回填）**：

| 坑 | 對策 |
|----|------|
| 帶旋轉的物件量邊界，AABB 被灌水縮放算錯 | 無旋轉狀態組裝＋量測，最後才轉向 |
| 真 HDR 全景用 1.0+ 曝光整片白 | HDR 曝光壓到 0.6～0.8 |
| 建築各軸拉伸 → 窗框變形一眼假 | 只等比縮放（照高度），縮完重驗賽道淨空 |
| Quaternius FBX 是 Z 朝上 → 整批人躺平 | 邊界判斷（垂直<水平×0.75）自動補轉 -90°X；**更好的做法：在 §4.7 的 Blender 步驟修軸向，別在 C# 補救** |
| Kenney 車體卡通比例 → 輪子縮進車殼 | 輪距取模型實寬；視覺輪放大 1.3 倍（物理半徑不動） |
| kenney.nl 下載連結是 JS 動態產生 | 瀏覽器點 Download 抓帶 hash 的 zip 連結再 curl |

**Unity 6 升級踩到的坑（2026-08-09 回填）**：

| 坑 | 對策 |
|----|------|
| 升級移除 `com.unity.textmeshpro`，而 `UnityEngine.UI` 本來是靠它間接帶進來的 → 用到 `Text` 的檔案整批 CS0246 | manifest 手動補 `com.unity.ugui`（版本對齊編輯器內建的那份）。沒用到 UI 的專案不會遇到，所以兩個專案表現會不一樣 |
| `PhysicMaterial` 改名 `PhysicsMaterial`（**error 級**，會擋住升級） | 直接改名；`PhysicMaterialCombine` 同理 |
| `rb.velocity`／`rb.drag`／`rb.angularDrag` 棄用 | → `linearVelocity`／`linearDamping`／`angularDamping` |
| `FindObjectOfType`／`FindObjectsOfType` 棄用 | → `FindAnyObjectByType()`／`FindObjectsByType()`。**注意 Unity 6.5 連 `FindObjectsSortMode` 都棄用了，不可帶該參數**——照舊資料改會改出新的警告 |
| `GraphicsSettings.renderPipelineAsset` 棄用 | → `defaultRenderPipeline` |
| URP 17.5 已移除 `m_EnableRenderGraph` | Compatibility Mode 廢止，Render Graph 是唯一路徑，不用也不能設 |
| URP 的欄位名在小版本間會變，照網路文章猜會寫錯 | 先寫一個 Report 方法用 `SerializedObject` 迭代實際欄位，確認後再寫設定程式（`U6Tools.Report` 就是範本） |

### 4.6 AI 3D 生成管線（v3.0 新增）

> 2026 的 image-to-3D 已可用（Tripo 約 8 秒出模、內建四邊面重拓樸與 auto-rig；Meshy 有 auto-rig 與動作預設；開源的 Hunyuan3D / TRELLIS 需要大顯卡，**GTX 1060 6GB 跑不動，只能用雲端網頁版**）。

**定位鐵則：AI 3D 是補位工具，不是主力。**

| 什麼時候用 | 什麼時候不用 |
|------|------|
| CC0 庫裡找不到的**關鍵特定物件**（招牌道具、主角專屬配件） | 材質、地表、天空（掃描資產永遠贏） |
| 需要「這個世界獨有」的辨識物 | 通用場景填充物（樹、桶子、路障——CC0 有更好的） |
| 概念快速試錯 | 大量重複物件（免費額度不夠，且風格難統一） |

**授權鐵則（最重要，牽涉上架）**：

- Meshy / Tripo **免費方案輸出為 CC BY 4.0**：可商用，**但必須標註來源**。
- 付費方案才給無標註的完整商用權。
- **2026-08-08 決議：只用免費額度，每一個 AI 生成模型當下就記進 `CREDITS.md`**（工具名、日期、用途、授權）。漏記一筆等於上架違規。
- 免費額度很小（例：Meshy 100 credits/月、一次完整生成約 20 credits ≈ 每月約 5 個模型），這也是它只能當補位的現實原因。

**五步管線**：

```
① 我開模型規格單 → ② 你在網頁生 → ③ 存進 _Assets\AI3D\ → ④ 我跑 Blender 後處理(§4.7) → ⑤ 進遊戲驗收
```

- **① 規格單**：物件名、用途、參考圖（可先用 §4.2 生一張正交參考圖再 image-to-3D，成功率遠高於純文字）、目標三角面數、是否需要 rig。
- **② 生成**：優先 image-to-3D（比 text-to-3D 穩定太多）；要人形就開 auto-rig。
- **③ 入庫**：`_Assets\AI3D\<批次>_<物件名>\`，同一批的參考圖與 prompt 一起放。**CREDITS.md 當下記一筆。**
- **④ 後處理**：一定要走 §4.7，AI 網格的三角面數、UV、法線、碰撞體都不能直接用。
- **⑤ 驗收**：跟 CC0 模型同框截圖比風格，對不上就別用。

### 4.7 Blender headless 後處理（v3.0 新增）

> 你已經有 Blender 可攜版，之前只拿來抓 Google Drive。它應該是管線的正式一站，跟 AI 圖後處理腳本同等地位。所有步驟走 `blender --background --python`，不開 GUI。

每個進遊戲的模型（CC0 或 AI 生成）都跑這一條：

| 步驟 | 做什麼 | 為什麼 |
|------|--------|--------|
| 1. 軸向與單位 | 統一 Y-up、1 unit = 1 m、Apply Transform | 根治「整批人躺平」「縮放算錯」 |
| 2. 三角面預算 | Decimate 到 §6.1 的預算內 | AI 生成常上 10 萬面，1060 撐不住 |
| 3. LOD | 產 LOD0 / LOD1(50%) / LOD2(20%) | 場景填充物的效能命脈 |
| 4. UV | 檢查 UV0；缺 lightmap UV 就自動展 UV1 | 沒有 UV1 就烘不了光 |
| 5. 法線與材質 | 重算法線、清空內嵌材質（引擎端重指定 URP Lit） | 避免匯入整批粉紅 |
| 6. 碰撞體 | 產凸包 `<name>_col` | 用網格碰撞體 = 效能自殺 |
| 7. 匯出 | FBX 到 `Assets/_Project/Art/<來源>/` | AssetPostprocessor 接手（§4.5） |

**原則同 §4.2**：原始檔永遠留在 `_Assets\` 不刪，處理過的才進 Unity 專案。腳本壞了可以重跑。

### 4.8 音訊管線（v3.0 新增）

> 舊版整份文件只有 P4 一句「音效」。手感的一半是聲音，這不能是事後才想的事。

| 類型 | 來源 | 進場時機 |
|------|------|----------|
| 核心操作音（打擊、碰撞、跳躍） | 免費 CC0 音庫（freesound CC0 篩選、Kenney 音效包） | **P1 就要有**——打擊感是玩法的一部分 |
| 環境音 / 氛圍 | CC0 音庫 | P2 |
| BGM | AI 生成或 CC0 | P2（垂直切片要有一首） |
| UI 音 | Kenney UI 音效包 | P2 |

**鐵則**：
- 授權跟素材同級管理，**每一段音訊入庫當下記 `CREDITS.md`**。AI 生成的音樂要確認該平台的商用條款，不確定就不用。
- 統一取樣率與格式；短音效 `Decompress on Load`，BGM `Streaming`。
- 音量在 AudioMixer 統一管理，不在各個 AudioSource 上散調。

### 4.9 授權與記錄

- CC0（Poly Haven / ambientCG / Kenney / Quaternius / Fab 免費 Megascans）：可商用、免標註，但**我們仍然記 CREDITS**，因為要能追溯來源重新下載。
- **CC BY（含 AI 3D 免費方案輸出）：可商用但必須標註**。標註義務在 `CREDITS.md`，P5 上架前逐條核對。
- **NC（非商業）絕不碰**，即使只是自己玩——之後想上架就得整批重做。
- AI 生圖：目前平台條款給使用者所有權可商用；但**核心視覺仍建議 AI + CC0 混用**，別讓整包美術繫在單一平台的條款上。
- **每批素材入庫當下**在 `CREDITS.md` 記一筆（AI 生成也記：工具、日期、批次名、授權）。

---

## 5. 防 BUG 品保流程（五道關卡）

> 誠實聲明：世界上沒有「零 bug」的遊戲，3A 也沒有。能做到的是——**bug 在寫完當下就被抓住，而不是累積到最後炸開**。以下五道關卡把這件事變成強制流程。
> v3.0 改動：舊版四關的關卡 2 只截靜態圖，驗不了物理與動態；也沒有任何一關看效能。新增關卡 3，並把關卡 2 升級。

### 關卡 1 — 編譯驗證（我做，每次改完程式）

- 我用 Unity batch mode **不開編輯器**直接編譯整個專案，讀 log 抓編譯錯誤，自己修到乾淨為止。
- 你永遠不會收到「我寫完了」但一按 Play 就滿屏紅字的東西。

### 關卡 2 — 畫面與動態驗證（我做，涉及畫面或物理的改動）

> ⚠️ **v3.1 重要修正：編輯器離線截圖驗證不了光照。**
> 編輯器裡手動 `Camera.Render()` 到 RenderTexture 的路徑**不套後處理，也不套 APV**（探針系統要執行期才初始化）。實測 APV 從沒烘到烘完整份探針資料，這條路徑拍出來的圖 SHA256 **完全一樣**——會讓人誤判成「功能沒效」或「改動沒生效」。
> **所以：素材上沒上、位置對不對 → 離線截圖夠用；光照、後處理、間接光 → 一律從打包後的執行檔截圖**（`PerfSampler` 會在暖機結束時自動拍一張）。

- **靜態**：batch mode 進場景算圖截圖，自己先看素材有沒有掛上、位置對不對、光照有沒有炸。
- **動態（v3.0 新增）**：涉及移動/物理的改動，改用**連拍序列**（每 N 個 fixed step 截一張，或錄短片）。一張靜態圖看不出車在抖、力士在滑、動畫在飄。
- 有問題我先修，修完的才給你過目。

### 關卡 3 — 效能與自測數據（我做，v3.0 新增；v3.1 修正量測方法）

每次交付附一組自動產出的數字，超標我先修：

| 指標 | 怎麼看 |
|------|--------|
| FPS / frame time（平均 + 1% low） | 對照 §6.1 預算。1% low 才是卡頓感的來源 |
| SetPass calls（平均 **與峰值**） | 峰值暴增＝實際遊玩會感覺到的卡頓，比平均值重要 |
| Tris / Verts | 對照三角面預算 |
| GC Alloc per frame | **目標 0**。每幀配置＝之後必然的卡頓 |
| 物理自測 log | 見下 |

**量測方法（v3.1 血淚修正，這段一定要照做）**：

1. **不能在編輯器 batch mode 的 Play 裡量。** 實測：12 秒只跑了 1 幀、frame time 13556 ms、draw call 取不到。batch mode 沒有真的繪圖迴圈，量出來的全是假的。
2. **要打包成獨立執行檔再量**（`PerfHarness.BuildBaseline` / `BuildOptimized` → 跑 exe 帶 `-perftest <tag>`）。這條路順便讓同一套工具能用在 P5 的打包驗證。
3. **release build 量時間，development build 讀計數器，兩者不可混用。**
   - `Draw Calls` 與 `GC Alloc` 的計數器只存在於 development build，release 量一定是「無資料」，不是程式壞了。
   - 但 development build（尤其開了 deep profiling）的額外開銷會蓋掉訊號——實測同一組設定在 release 是「開功能快 12%」，在 dev build 卻變成「慢 13%」。**拿 dev build 比時間會得到相反的結論。**
4. **取樣前要暖機**（目前 4 秒），並在取樣時**關掉垂直同步與幀率上限**（`vSyncCount = 0`、`targetFrameRate = -1`），否則 60 FPS 天花板會把差異整個藏起來。
5. 場景還很空的時候，數字只能當「相對比較」，不能當「效能夠用」的證明。P3 量產後必須重測。

**物理自測場景（鐵則）**：每個物理系統都要有一個 headless 自測場景，跑固定腳本輸出數值 log（DriftGame 的 `DriveSelfTest.cs` 就是範本，這是本專案目前最強的一環）。它抓的是「肉眼看不出但數字不對」的 bug——力沒被登記、加速度不符理論值、參數改了卻沒生效。

**自動回歸測試（v3.0 新增）**：純數值邏輯（計分、體力、傷害、解鎖條件）寫 EditMode 測試，我在 batch mode 幾秒跑完。這些是「人腦回歸抽查」最容易漏、又最好自動化的部分。

### 關卡 4 — 實玩檢查清單（你做，每個功能完成時）

我每交付一個功能，會附一張**該功能的 5 分鐘檢查清單**，固定包含三類：

1. **正常路徑**：照設計玩一次，行為符合預期。
2. **故意亂玩**（最重要——你自己玩永遠用「對的方式」，bug 都藏在錯的方式裡）：
   - 在邊界/牆角/斜坡做這個動作
   - 動作做到一半按 Esc 暫停、切視窗、再回來
   - 連打按鍵、同時按互斥的鍵、在讀取中狂按
   - 數值推極限（速度最快時、體力歸零時、分數溢出時）
3. **回歸抽查**：這次改動**鄰近的舊功能**挑 2 項重玩。

發現問題 → 用一句話記進 `BUGS.md`（現象 + 怎麼觸發），我來修。**修完的 bug 我會回測清單上的鄰近項目才交還。**

### 關卡 5 — 提交、備份與回退（我做，通過前四關後）

- 通過驗收 → `git commit`（訊息寫清楚做了什麼）。
- 里程碑完成 → `git tag P2-vertical-slice` 之類。
- **收工 → `git push` 到遠端（v3.0 新增，目前最大的缺口：只有本機 git，硬碟壞了三個專案一起走）。**
- 任何時候改壞了：回上一個 commit，最多損失一個功能的工時。

### 節奏規則

- **小步提交**：一個 commit 只做一件事。一次改十個系統 = bug 來源查不出來。
- **Console 零容忍**：黃色警告也要清。
- **不帶病前進**：`BUGS.md` 有未修的「會壞流程」級 bug 時，不開新功能。

---

## 6. 技術規格（鐵則）

### 6.1 共通與效能預算

- 貼圖一律 **2 的次方**（512 / 1024 / 2048），非 2 次方的圖在匯入時規整。
- 檔名英文小寫 + 底線：`tire_smoke_01.png`。**中文/全形檔名會在 Blender Python、命令列、部分工具鏈炸掉。**
- 成品素材放 `Assets/_Project/`，原始檔放專案外的 `_Refs\` / `_Assets\`。

**效能預算表（P0 定、P2 驗、P4 收尾；基準機 GTX 1060 6GB / i7-7700 / 16GB）**：

| 指標 | 2D | 3D（B 檔半寫實） |
|------|-----|------|
| 目標 | 60 FPS 穩定（16.6 ms） | 60 FPS 穩定 |
| 畫面三角面 | — | ≤ 150 萬 |
| SetPass calls | ≤ 100 | ≤ 200 |
| 單物件三角面 | — | 主角 ≤ 15k、道具 ≤ 5k、填充物 ≤ 2k |
| 貼圖 | 512～1024 | 1024～2048（8K 掃描圖匯入時降到 2K） |
| VRAM | — | ≤ 3.5 GB（留餘裕，1060 只有 6GB 且系統要吃） |
| GC Alloc / frame | 0 | 0 |

### 6.2 2D 專用

- **全專案統一 PPU**，開工時定一次，之後不改。
- 像素風：Filter Mode = **Point**，Compression = None；手繪風：Bilinear 可。
- 同場景的 sprite 進 **Sprite Atlas**。
- 分層背景（視差捲動）每層獨立一張。

### 6.3 3D 專用

**光照與後處理清單（P2 逐項打勾，畫面質感 50% 在這，先做這個再談素材）**：

- [ ] Color Space = Linear
- [ ] HDR 開啟
- [ ] Tonemapping = ACES（或 Neutral，B 檔兩者都可，選一個之後不換）
- [ ] 真 HDRI 環境光（Poly Haven，曝光 0.6～0.8）
- [ ] Reflection Probe（半寫實的金屬/濕地面沒這個會像塑膠）
- [ ] SSAO（接觸陰影，最便宜的「有重量」感）
- [ ] Shadow distance 與 cascade 依場景大小調（不是用預設值）
- [ ] 靜態物件烘焙 GI；動態物件用 Light Probe
- [ ] Bloom / Vignette / Color Grading 收尾（別過頭）

**Adaptive Probe Volumes 實戰（2026-08-09 回填，SumoParty 驗證）**：

APV **不是一個開關，是要重新規劃場景光照**。踩過的三個坑都屬於「看起來成功、其實沒做事」：

| 坑 | 症狀 | 對策 |
|----|------|------|
| 場景沒東西可烘 | 開了 APV、烘焙也「完成」，但畫面零變化。實際情況：6 個光源全 Realtime、361 個物件全部未標記 Contribute GI | 光源改 **Mixed**（直接光仍即時、保留動態陰影）、靜態物件標 **Contribute GI**，再烘 |
| 標記靜態時順手加了 Batching Static | 靜態批次會把物件排除在 GPU Resident Drawer 之外，等於把 SetPass 的改善吐回去 | 只加 `ContributeGI \| OccluderStatic \| OccludeeStatic \| ReflectionProbeStatic`，**不要 BatchingStatic**。APV 走探針，也不需要 lightmap UV（設 `receiveGI = LightProbes`） |
| `AdaptiveProbeVolumes.BakeAsync()` 配 `-quit` | 非同步烘焙被半路砍掉，`Lightmapping.isRunning` 追蹤不到它所以等待迴圈直接放行。log 只有 `Baking Adaptive Probe Volumes` 沒有 `Generating Probe Volume Bricks`，Baking Set 停在 3 KB | batch mode 用**同步**的 `Lightmapping.Bake()`。烘成功的樣子是產出 `CellData.bytes` / `CellSupportData.bytes` 等數 MB 的檔案 |

**驗收 APV 是否真的生效，看三件事**（缺一都可能是假的）：
1. `Assets/.../<場景> Baking Set*.bytes` 有數 MB 的探針資料，不是只有幾 KB 的設定檔
2. 打包後的 build 檔案大小明顯變大（SumoParty 實測 104 MB → 109 MB）
3. **從 build 截圖**比對，不是從編輯器離線截圖（見 §5 關卡 2）

**誠實結論（SumoParty 現況）**：APV 已正確啟用、烘焙、隨 build 出貨，效能成本在雜訊範圍內（1.57 → 1.53 ms），**但畫面看不出差別**。因為這個場景是暗場地 + 土俵聚光燈，沒有值得反彈的間接光。APV 的價值要在有亮面、天光或彩色環境時才出得來——那是重新規劃打光的美術決策，不是設定問題。

**其他鐵則**：

- 材質統一管理 Metallic / Smoothness，不放預設值。**半寫實的關鍵在 Smoothness 的變化**，全部同一個值就會像塑膠。
- 風格統一 > 模型精細：全部同一檔位 > 「主角精細背景方塊」。
- 模型來源優先序（B 檔）：Fab 免費 Megascans / Poly Haven → AI image-to-3D 補缺 → Blender 腳本生成 → 手工建模。
- 開 GPU Instancing；重複物件用 LOD。

### 6.4 物理鐵則（v3.0 新增）

> 舊版整份文件對物理零規範，但 DriftGame 和 SumoParty 的核心樂趣 100% 在物理手感。

**專案物理設定（2026-08-08 已套用並驗證）**：

| 設定 | 預設值 | DriftGame | SumoParty | 為什麼 |
|------|--------|-----------|-----------|--------|
| Solver Type | PGS | **TGS Soft** | **TGS Soft** | 車輛、堆疊、互推、關節的穩定度差很多，改一個下拉選單的事 |
| Solver Iterations | 6 | **12** | **8** | 高速接觸與互推抖動的主因 |
| Solver Velocity Iterations | 1 | **2** | **2** | 反彈與分離的收斂 |
| Improved Patch Friction | off | **on** | **on** | 摩擦更接近真實 |
| Fixed Timestep | 0.02 (50Hz) | **0.01 (100Hz)** | **0.0167 (60Hz)** | 高速甩尾在 50Hz 會穿牆、抓地不連續 |
| Maximum Allowed Timestep | 0.333 | **0.1** | **0.1** | 卡頓時避免物理「補跑」一大堆步造成爆炸 |

> 改 Fixed Timestep 前必須確認：所有持續施力用 `ForceMode.Force/Acceleration`，或有乘 `Time.fixedDeltaTime`。**每個 FixedUpdate 用 `ForceMode.VelocityChange/Impulse` 加速度的寫法，改 timestep 會直接讓手感翻倍或減半。**（本次已逐一確認兩專案安全：CarController 走 `AddForceAtPosition` + Force，SumoWrestler 的 VelocityChange 都是一次性事件、移動用 `Lerp(…, k * Time.fixedDeltaTime)`。）

**寫程式時的鐵則**：

1. **所有力在 `FixedUpdate`**；輸入在 `Update` 讀並快取，不要在 `Update` 施力。
2. **快速物體**：`Interpolate` + `Collision Detection = Continuous Dynamic`。慢速物體不要開（浪費）。
3. **質心手動設**：車輛壓到底盤下方、角色壓低。預設質心＝翻車與飄浮的頭號來源。
4. **質量用真實量級**（車 ~1200kg），力矩數字才有物理意義、才好調。
5. **不要在 `Update` 直接改 Rigidbody 的 transform**——會跟求解器打架。用 `MoveRotation` / `MovePosition` 在 `FixedUpdate` 做。
   > ⚠️ 已知待修：`SumoWrestler.cs` 的自動轉向目前寫在 `Update` 直接設 `transform.rotation`，接觸時會跟求解器搶控制權。列入 `BUGS.md`。
6. **車輛不用 WheelCollider**：實測會把車鎖死。用射線懸吊 + 自算胎面力（DriftGame 的 Pacejka 實作是本專案範本）。
6b. **Cylinder primitive 的碰撞體是 CapsuleCollider，不是圓柱**（2026-08-09 一天踩兩次）：
   - 當地板/平台用時是「圓頂」，東西站上去會自己滑走（土俵實測靜置漂移 1.7m）
   - 更陰險的是壓扁的大圓盤：CapsuleCollider 最小高度＝直徑，scale (22, 0.05, 22) 會變成
     **半徑 11m 的隱形圓頂**穿出地面把整個場景頂在弧面上（實測漂移 8.9m）
   - 對策：凡是 Cylinder primitive 一律 `DestroyImmediate(collider)`，平面用 BoxCollider、
     精確圓柱用 MeshCollider（靜態物件可用非凸面）
7. **所有手感參數進 ScriptableObject**，調參數不改程式碼、不重編譯。
8. **每個物理系統配一個 headless 自測場景**（§5.3）。

---

## 7. 檔案與資料夾規範

### 7.1 專案根目錄（`E:\下載\CLAUDE\ｇａｍｅ\`）

```
Claude-Code遊戲製作流程-最終版.md   ← 本文件（怎麼做）
<遊戲名>企劃書.md                    ← 各遊戲（做什麼），頂部必有「狀態欄」
AI作圖指令模板.md                    ← prompt 範本庫
CREDITS.md                          ← 素材來源與授權記錄（含 AI 批次、CC BY 標註義務）
BUGS.md                             ← 未修 bug 清單
_Refs\                              ← AI 原圖 + 參考圖 + PROMPTS.md
_Assets\                            ← CC0 模型/HDRI/AI 3D 原始包（gitignore）
<UnityProject>\                     ← 各 Unity 專案（英文名）
```

> ⚠️ **已知技術債（v3.0 標記）**：根目錄是全形 `ｇａｍｅ`、專案 `蚯蚓的一生` 是中文名——違反 §6.1 自己的鐵則。目前 Unity/git 沒事，但 Blender headless（§4.7）與部分命令列工具會踩到。處理方式：新專案一律英文；既有的等有空窗期再搬（搬動要重設 .lnk 捷徑與 git remote）。

### 7.2 `_Refs\` 與 `_Assets\` 結構

```
_Refs\PROMPTS.md        ← 風格鎖句 + 每批素材的完整 prompt
_Refs\01_風格參考\  02_粒子貼圖\  03_UI圖示\  04_材質貼圖\  05_天空盒\  06_宣傳圖\
_Refs\07_角色sprite\  08_場景背景\        ← 2D 專案加開

_Assets\<來源名>\       ← CC0 原始包（Kenney / Quaternius / PolyHaven / Fab）
_Assets\AI3D\<批次>_<物件名>\   ← AI 生成模型 + 參考圖 + prompt（v3.0 新增）
```

### 7.3 文件維護規則

1. **狀態欄要活的**：每份企劃書頂部的狀態和里程碑 checkbox，做完當下就更新。文件跟現實脫節比沒有文件更糟。
2. **決定要回填**：任何「待你決定」的事項有了答案，當天寫回文件，附日期。
3. **環境事實只記一處**：Unity 版本、路徑等記在專案 CLAUDE.md，企劃書不重複記。
4. **記錄當下寫**：CREDITS / PROMPTS / BUGS 都是「事情發生當下記一筆」，事後補一定漏。
5. **本文件自己也要活**（v3.0 新增）：踩到新坑當下回填，過期段落當下刪。

---

## 8. 每次開工的標準迴圈

```
1. 我讀進度（企劃書狀態欄 + git log + BUGS.md）→ 跟你確認今天目標
2. 動工（我寫程式/生素材/跑管線）
3. 關卡 1+2+3（我自己編譯 + 動態截圖 + 效能與自測數據）
4. 交付：給你「改了什麼 + 截圖 + 效能數字 + 5 分鐘檢查清單」
5. 你實玩回報 → 有 bug 我修（修完回測）
6. 通過 → commit → push → 更新企劃書狀態欄 → 收工
```

### 你可以用的口令

| 你說 | 我做 |
|------|------|
| 「開始 P〇」 | 從該階段第一項動工 |
| 「生圖：〇〇」 | 開規格單 → 批次生圖 → 跑後處理管線 |
| 「生模型：〇〇」 | 開模型規格單 → 告訴你網頁怎麼生 → Blender 後處理 → 進遊戲（§4.6） |
| 「驗收」 | 跑編譯 + 動態截圖 + 效能數字 |
| 「跑物理自測」 | 跑 headless 自測場景，出數值 log |
| 「有 bug：〇〇」 | 記進 BUGS.md → 修 → 回測 → 回報 |
| 「進度」 | 報目前階段、已完成、下一步 |
| 「存檔」 | commit + push + 必要時打 tag |

---

## 9. 風險對照表

| 風險 | 對策（本文件哪一節擋它） |
|------|--------------------------|
| 不好玩才發現，美術白做 | P1 灰盒驗收先於一切美術（§3） |
| **畫質目標沒定，美術做兩次** | **P0 畫質檔位決策（§3）** |
| AI 素材風格漂移，像雜牌拼盤 | 風格鎖句 + 同批生產 + 對照驗收（§4.3） |
| **AI 生成模型直接進遊戲拖垮效能** | **Blender 後處理 + 三角面預算（§4.7 / §6.1）** |
| **物理手感抖、飄、穿牆** | **物理鐵則與求解器設定（§6.4）** |
| **效能問題到最後才發現** | **P0 定預算、P2 驗、關卡 3 每次量（§6.1 / §5.3）** |
| bug 堆到最後炸開 | 每功能五道關卡，不帶病前進（§5） |
| 改壞回不去 | 小步 commit + 里程碑 tag（§5 關卡 5） |
| **硬碟壞掉全部沒了** | **收工 push 遠端（§5 關卡 5）** |
| 範圍膨脹做不完 | 三根柱子過濾功能 + 階段驗收制（§3） |
| 文件與現實脫節 | 狀態欄活更新 + 決定回填（§7.3） |
| **CC BY 素材漏標註，上架違規** | **入庫當下記 CREDITS + P5 逐條核對（§4.9）** |
| 三專案並行全部爛尾 | 明訂主線專案，其餘凍結（見 §10） |

---

## 10. 現況與下一步（2026-08-09 更新）

**現況**：

| 專案 | 引擎 | 階段 | 狀態 |
|------|------|------|------|
| SumoParty | **6000.5.6f1 / URP 17.5** | P2 垂直切片（3D 觀眾席完成） | **主線** |
| DriftGame | **6000.5.6f1 / URP 17.5** | P2 場館版 | 暫緩 |
| 蚯蚓的一生 | 2022.3.36f1 | P1 | 凍結（升級待辦） |

備份：GitHub public repo `flyingwen17-collab/game-projects`，含 `pre-unity6-sumoparty` / `pre-unity6-driftgame` 還原點。

**SumoParty 效能基準線**（1280×720、release build、暖機 4s + 取樣 12s）：

| | 關功能 | 開功能（STP + GPU Resident Drawer） |
|---|---|---|
| 平均 frame time | 1.83 ms（547.9 FPS） | **1.61 ms（621.4 FPS）** |
| 1% low FPS | 410.4 | 418.4 |
| SetPass 平均／峰值 | 38 ／ **3996** | 37 ／ **44** |
| 三角面 | 1.14 M | 1.12 M（§6.1 預算 1.5 M 內）|

最大收益是 SetPass 峰值被抹平（卡頓尖峰的來源），不是平均 FPS。
⚠️ 場景仍空，離 60 FPS 天花板很遠，**P3 量產後必須重測**。

**已完成（v3.0 → v3.1）**：
- [x] 定畫質檔位 = B 半寫實
- [x] 物理設定套用到兩專案並確認腳本安全（§6.4）
- [x] AI 3D 生成納入流程，授權規則明訂（§4.6 / §4.9）
- [x] 兩專案升 Unity 6，API 全部清乾淨，**0 error 0 warning**
- [x] URP 功能：STP + Render Scale 0.75 + GPU Resident Drawer + GPU Occlusion Culling + Reflection Probe Blending/Box Projection
- [x] §5 關卡 3 量測 harness（打包 → 跑 exe → 吐數字 → 附截圖），已產出真實基準線
- [x] APV 啟用並烘焙（但此場景看不出差別，原因見 §6.3）
- [x] 遠端備份

**下一步（依投報率排序）**：

1. **修每幀 GC 配置**（`BUGS.md`）——目前約 2000 bytes/frame、峰值 231 KB，違反 §6.1 鐵則。場景還空所以不明顯，P3 會浮現。
2. **`SumoWrestler` 在 `Update` 直接設 `transform.rotation`**（`BUGS.md`）——跟物理求解器搶控制權，改 `MoveRotation`。
3. **重新規劃 Arena 打光**，讓 APV 有東西可反彈（亮面／天光／彩色環境），畫質才吃得到 B 檔標準。需要你的美術判斷。
4. **建 §4.7 的 Blender headless 後處理腳本**——AI 3D 與 CC0 模型進遊戲前的必經站，目前還是手動。
5. **蚯蚓的一生升 Unity 6**（照 §4.5 的坑表走，會比前兩次快很多）。
6. 依 §6.3 清單把光照後處理逐項補齊到 B 檔標準。

---

## 版本歷程

| 版本 | 日期 | 主要改動 |
|------|------|----------|
| v3.1 | 2026-08-09 | 關卡 3 量測方法血淚修正（batch mode 量不到效能，必須打包成 exe；release 量時間、dev 讀計數器）；關卡 2 修正（離線截圖驗證不了光照與後處理，要從 build 截圖）；回填 Unity 6 升級的七個坑與 APV 的三個坑；兩專案升級完成後回填現況 |
| v3.0 | 2026-08-08 | 畫質檔位決策；AI 3D 生成管線與授權鐵則；Blender headless 後處理；物理鐵則（含實際設定）；效能預算與關卡 3；音訊管線；遠端備份；修正過期內容 |
| v2.1 | 2026-08-04 | CC0 3D 模型管線（B/D 階段實戰回填） |
| v2.0 | — | AI 生圖進遊戲管線、四道品保關卡 |
