# 搬家到新電腦 — 完整清單

> 建立日期：2026-08-09
> 舊機器：i7-7700 / GTX 1060 6GB / 16GB（跑不動了）
> 這份文件本身在 git 裡，新電腦 clone 下來就看得到。

---

## 0. 一句話版本

**程式碼和文件全在 GitHub，clone 就好。真正需要手動搬的只有兩個資料夾 `_Refs\` 和 `_Assets\`，加上 Claude 的記憶資料夾。**

---

## 1. 已經安全的東西（在 GitHub 上，不用搬）

倉庫：`https://github.com/flyingwen17-collab/game-projects`（public）

包含：
- 三個 Unity 專案的全部程式碼、場景、素材、ProjectSettings
- `Claude-Code遊戲製作流程-最終版.md`（v3.1）、各遊戲企劃書
- `CREDITS.md`、`BUGS.md`、`AI作圖指令模板.md`
- 還原點 tag：`pre-unity6-sumoparty`、`pre-unity6-driftgame`

新電腦上：

```bash
git clone https://github.com/flyingwen17-collab/game-projects.git
```

---

## 2. 必須手動搬的東西（不在 git 裡）

| 來源路徑（舊電腦） | 大小 | 為什麼不能不搬 |
|---|---|---|
| `E:\下載\CLAUDE\ｇａｍｅ\_Refs\` | 92 MB | **AI 生圖的原圖 + `PROMPTS.md` 風格鎖句。這是唯一一份，重生不出一模一樣的圖，風格會漂移**（流程 MD §4.3） |
| `E:\下載\CLAUDE\ｇａｍｅ\_Assets\` | 200 MB | Kenney / Poly Haven / Quaternius 的 CC0 原始包。**理論上可依 `CREDITS.md` 重新下載**，但直接複製省事得多 |
| `C:\Users\2022.06.18\.claude\projects\E-----CLAUDE-----\memory\` | 很小 | Claude 的跨對話記憶（踩過的坑、專案慣例）。不搬就等於失憶重來 |

**一鍵複製到外接碟**（把 `D:` 換成你的隨身碟代號，在舊電腦的 PowerShell 執行）：

```powershell
$dst='D:\game-搬家'
robocopy 'E:\下載\CLAUDE\ｇａｍｅ\_Refs' "$dst\_Refs" /E /R:1 /W:1
robocopy 'E:\下載\CLAUDE\ｇａｍｅ\_Assets' "$dst\_Assets" /E /R:1 /W:1
robocopy 'C:\Users\2022.06.18\.claude\projects\E-----CLAUDE-----\memory' "$dst\claude-memory" /E /R:1 /W:1
```

到新電腦後把 `_Refs` 和 `_Assets` 放回 clone 出來的專案根目錄，`claude-memory` 放回對應的 `.claude\projects\<專案雜湊>\memory\`。

---

## 3. 不用搬的東西（會自動重建，搬了只是浪費時間）

- 各專案的 `Library\`、`Temp\`、`obj\`、`Logs\`、`UserSettings\` — Unity 開專案時自動重建
- `Build\`、`Builds\` — 重新打包即可
- `Screenshots\` — 重新產生即可
- `*.sln`、`*.csproj` — Unity 自動產生

---

## 4. 新電腦要裝的軟體

| 軟體 | 版本 | 用途 | 備註 |
|---|---|---|---|
| **Unity 6000.5.6f1** | 就是這個版本 | SumoParty、DriftGame | 版本不同會觸發專案升級，不要用別的版本開 |
| **Unity 2022.3.36f1** | 就是這個版本 | 蚯蚓的一生（還沒升級） | 想省事可以先照 §4.5 的坑表把它也升到 Unity 6，就只需要裝一個 |
| Unity Hub | 最新 | 管理上面兩個 | |
| Git for Windows | 最新 | 版本控制 + 認證管理員 | |
| GitHub CLI (`gh`) | 選配 | `winget install --id GitHub.cli -e` | |
| **Blender 4.5 可攜版** | 4.5.x | §4.7 的 headless 模型後處理 | 舊機器放在 `E:\Tools\blender-4.5.12-windows-x64`，含 Python + gdown。可直接複製整個資料夾，或重新下載 |

Unity 安裝時要勾的模組：**Windows Build Support (IL2CPP)**（打包 exe 用）。

---

## 5. 搬完後的驗收步驟

照做一遍，確認沒搬丟：

1. **編譯驗證**（不用開 GUI）：
   ```powershell
   & 'C:\Program Files\Unity\Hub\Editor\6000.5.6f1\Editor\Unity.exe' -batchmode -quit -nographics -projectPath '<新路徑>\SumoParty' -logFile "$env:TEMP\verify.log"
   ```
   log 裡 `error CS` 和 `warning CS` 都應該是 0（升級時已清乾淨）。

2. **打包可玩版本**（三個專案各跑一次）：
   ```powershell
   & '<Unity路徑>\Unity.exe' -batchmode -quit -projectPath '<專案路徑>' -executeMethod GameBuild.BuildPlayable -logFile "$env:TEMP\build.log"
   ```
   產出在 `<專案>\Build\<專案名>.exe`。

3. **重建「開始遊戲」捷徑**（舊電腦上的捷徑指向舊 Unity 編輯器，是壞的，不要搬）：
   ```powershell
   $root='<新路徑>'   # 例如 D:\game
   $sh = New-Object -ComObject WScript.Shell
   $desk = [Environment]::GetFolderPath('Desktop')
   foreach ($g in @(@('SumoParty','相撲'), @('DriftGame','甩尾'), @('蚯蚓的一生','蚯蚓'))) {
     $exe = Join-Path $root "$($g[0])\Build\$($g[0]).exe"
     if (-not (Test-Path $exe)) { Write-Host "跳過 $($g[0])：還沒打包"; continue }
     foreach ($p in @((Join-Path $root "$($g[0])\!開始遊戲.lnk"), (Join-Path $desk "$($g[1])遊戲.lnk"))) {
       $l = $sh.CreateShortcut($p)
       $l.TargetPath = $exe
       $l.WorkingDirectory = Split-Path $exe
       $l.Save()
     }
   }
   ```

4. **效能重測**（新機器的基準線一定不同，舊數字作廢）：
   ```powershell
   & '<Unity路徑>\Unity.exe' -batchmode -quit -projectPath '<路徑>\SumoParty' -executeMethod PerfHarness.BuildOptimized -logFile "$env:TEMP\p.log"
   & '<路徑>\SumoParty\Builds\optimized\SumoParty.exe' -perftest optimized -screen-width 1280 -screen-height 720
   ```
   結果寫在 exe 旁邊的 `perf_optimized.txt`，順便附一張 `shot_optimized.png`。

---

## 6. 搬家時順手該知道的「進行中」狀態

**做完的**（流程 MD §10 有完整清單）：
- 流程 MD v3.1、兩專案升 Unity 6 且 0 error 0 warning、URP 功能全開、物理設定、效能量測 harness、APV、GitHub 備份

**做到一半的**：
- **三個專案的可玩版本沒打包成功** —— 舊機器在打包途中撐不住了。新電腦照 §5 步驟 2 跑一次就有了
- **「開始遊戲」捷徑因此也還沒建** —— 照 §5 步驟 3

**待辦（依投報率，流程 MD §10）**：
1. 修每幀 GC 配置（約 2000 bytes/frame，見 `BUGS.md`）
2. `SumoWrestler` 在 `Update` 設 `transform.rotation` 跟物理求解器搶控制權（見 `BUGS.md`）
3. **決定 SumoParty 要不要改成真實推撞物理** —— 目前力士是每幀覆寫 `linearVelocity` 的角色控制器，不是真的質量對抗（詳見下方「新機器上第一件該想的事」）
4. 重新規劃 Arena 打光，讓 APV 有東西可反彈
5. 建 §4.7 的 Blender headless 後處理腳本
6. 蚯蚓的一生升 Unity 6
7. **3D 素材還停在 A 檔卡通低模**，B 檔半寫實一張都還沒進（Kenney / Quaternius 做不出半寫實，要換 Fab Megascans / Poly Haven）

---

## 7. 新機器上第一件該想的事

舊機器（GTX 1060 6GB / 16GB / i7-7700）是「B 檔半寫實」這個目標的天花板來源。**新電腦的規格會直接改變 §3 的畫質檔位決策**：

- 如果新機器有 RT core（RTX 系列）+ 12GB 以上 VRAM + 32GB RAM → 可以重新考慮 C 檔照片寫實 / HDRP
- 如果差不多或略好 → 維持 B 檔，把力氣花在素材來源升級（Fab Megascans）而不是引擎管線

**新電腦到手後，把規格告訴 Claude，重跑一次 §3 的畫質檔位決策，再決定後面的素材路線。** 在那之前不要開始換素材，不然可能白做。
